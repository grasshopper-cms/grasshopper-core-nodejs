/**
 * Middleware that will list all of the assets in a node
 *
 * Ex: {nodeid: nodeid }
 *
 * @param kontx
 * @param next
 */
module.exports = function list(kontx, next){
    'use strict';


    next();
};