/**
 * Middleware that will delete a named asset from within a node.
 *
 * Ex: {nodeid: nodeid, filename: filename}
 *
 * @param kontx
 * @param next
 */
module.exports = function deleteAsset(kontx, next){
    'use strict';


    next();
};