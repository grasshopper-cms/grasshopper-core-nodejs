/**
 * Middleware that will load the details about a specific asset
 *
 * Ex: {nodeid: nodeid, filename: filename}
 *
 * @param kontx
 * @param next
 */
module.exports = function find(kontx, next){
    'use strict';


    next();
};