/**
 * Middleware that will delete all of the assets within a node.
 *
 * Ex: {nodeid: nodeid}
 *
 * @param kontx
 * @param next
 */
module.exports = function deleteAll(kontx, next){
    'use strict';


    next();
};