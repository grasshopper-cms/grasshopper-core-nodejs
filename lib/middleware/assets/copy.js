/**
 * Middleware that will copy one asset from one node to another
 *
 * Ex: {nodeid: nodeid, newnodeid: newnodeid, filename: filename}
 *
 * @param kontx
 * @param next
 */
module.exports = function copy(kontx, next){
    'use strict';


    next();
};